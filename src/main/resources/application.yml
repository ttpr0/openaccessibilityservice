##### General server settings #####
server:
  port: 5001
  error:
    whitelabel:
      enabled: false
    include-stacktrace: never
    include-message: always

##### Logging settings #####
logging:
  level:
    root: WARN
    org:
      tud:
        oas: DEBUG
      heigit:
        ors: DEBUG
